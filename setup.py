from setuptools import setup

setup(name='curried',
      url='http://github.com/tripped/curried',
      author='Trip Volpe',
      author_email='trip.volpe@gmail.com',
      vcversioner={},
      setup_requires=['vcversioner'],
      packages=['curried'])
